<% if(locals.task) { %>
  <h1><%= task.name %></h1>
  <div>
    Opis zadania: <%= task.description %>
  </div>
  <div>
    termin: <%= task.completion_date %>
  </div>
  <div>
    status: <%= task.status %>
  </div>
<% } else { %>
  <h1>No task with this id exists!</h1>
<% } %>